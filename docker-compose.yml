services:
  fleet-portal:
    build: .
    container_name: fleet-portal
    restart: unless-stopped
    ports:
      - "4000:3000"
    environment:
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongo:27017/thehub
    networks:
      - thehub_default
      - internal

networks:
  thehub_default:
    external: true
  internal:
    driver: bridge
